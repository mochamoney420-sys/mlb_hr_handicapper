name: Daily MLB HR Prediction Pipeline

on:
  schedule:
    - cron: '0 11 * * *'
  workflow_dispatch:

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository code
      # Use a published stable version of the checkout action
      uses: actions/checkout@v3

    - name: Set up Python environment
      # Use a stable published version of setup-python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install required system packages
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy xgboost requests pybaseball

    - name: Execute handicapping model script
      env:
        DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
      run: python run_daily_predictions.py
